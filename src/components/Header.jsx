import React from 'react';

function Header() {
  var headerStyle={
    color: 'blue',
    textAlign: 'center',
  };
  return(
    <div>
      <h1 style={headerStyle}>Tap Room</h1>
    </div>
  );
}

export default Header;
